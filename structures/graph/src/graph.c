#include <stdlib.h> // malloc
#include <stdio.h> // printf
#include "graph.h"

/* when inserting a new element into the graph
 * we find x in our list of edges then create
 * a new edge node representing it's connection
 * to y, and bind that to the end of x's
 * edge list.
 */
void graph_insert(*graph, int x, int y) {
  graph->edges[x]
}

int main() {
  return 0;
}
